---
permalink: version.json
---

{
  "name": "{{ pkg.name }}",
  "version": "{{ pkg.version }}",
  "homepage": "{{ pkg.homepage }}",
  "repository": "{{ pkg.repository.url }}",
  "sha": "{{ git.long }}",
  "branch": "{{ git.branch }}"
}
